meta {
  name: Transfer Decline
  type: http
  seq: 37
}

delete {
  url: {{baseUrl}}/applets/{applet_id}/transferOwnership/{key}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
