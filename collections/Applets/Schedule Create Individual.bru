meta {
  name: Schedule Create Individual
  type: http
  seq: 24
}

post {
  url: {{baseUrl}}/applets/{applet_id}/events/individual/{respondent_id}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
