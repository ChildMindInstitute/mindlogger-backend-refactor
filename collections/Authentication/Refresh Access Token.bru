meta {
  name: Refresh Access Token
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/auth/token/refresh
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "refreshToken": ""
  }
}
