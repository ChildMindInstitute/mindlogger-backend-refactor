meta {
  name: Upload
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/file/upload
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  file: 
}
